package java_my_practice;

import java.util.Arrays;

public class SaimTask {
    public static void main(String[] args) {
        /**
         * 100numberarray1.Create an int array called numbers that has length of 1002.Assign
         * 1-100 to each index
         * of the array.  Then use a for each loop to print out all the elements of the array
         */
        int[] numbers = new int[100]; //we declare size of array
//            for (int i = 0; i < numbers.length; i++) {
//               numbers[i] += i +1;
//
//        }
//        System.out.print(Arrays.toString(numbers));
        for (int each : numbers) {
            System.out.println();
        }
    }

}
